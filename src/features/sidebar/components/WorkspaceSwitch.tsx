"use client";

import React, { useState } from "react";

const WorkspaceSwitch = () => {
  const [currentWorkspace, setCurrentWorkspace] = useState(null);

  return <div>WorkspaceSwitch</div>;
};

export default WorkspaceSwitch;
